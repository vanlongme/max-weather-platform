name_prefix = "max-weather"

vpc_id                = "vpc-REPLACE"
public_subnet_id      = "subnet-REPLACE"
instance_profile_name = "max-weather-jenkins"
key_name              = "max-weather-key"

allowed_cidrs = ["YOUR_PUBLIC_IP/32"]

instance_type    = "t3.medium"
root_volume_size = 30

tags = {
  Project     = "max-weather"
  Environment = "poc"
  ManagedBy   = "terraform"
}
