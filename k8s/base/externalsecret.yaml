apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: weather-app-config
  labels:
    app: weather-api
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore
  target:
    name: weather-app-config
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: max-weather/staging/app-config
